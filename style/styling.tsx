import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
   body: {
      padding: 20,
   },
   input: {
      borderWidth: 1,
      borderColor: 'gray',
      padding: 10,
      marginBottom: 10,
   },
   greenText: {
        color: 'green',
   }
})

export default styles;